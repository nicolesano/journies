<h1>Add an entry</h1>

<%= simple_form_for [@entry], url: journal_entries_path do |f| %>
    <%= f.input :title %>
    <%= f.input :content %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Finish entry", class: "btn" %>
<% end %>